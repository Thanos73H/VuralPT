1. app.js: photo() now loads assets/info/{exercise-id}.svg instead of the old root assets/*.svg path.
2. service-worker.js: cache version bumped to vuralpt-v3-images so the browser does not keep the old app.js cached.
No exercise content, program logic, HTML, or CSS was changed.
